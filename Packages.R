# Script for important packages, easily download/update

important_packages <- c(
  'tidyverse',
  'caret',
  'xtable',
  'stargazer'
)

install.packages(important_packages)

# library(important_packages)